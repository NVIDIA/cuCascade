---
phase: 03-test-migration-and-build-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: true
requirements:
  - TEST-09
  - TEST-10

must_haves:
  truths:
    - "pixi run build compiles all 61 targets with zero errors"
    - "pixi run test passes all tests with zero failures"
    - "No synchronized_data_batch references remain anywhere in the test directory"
  artifacts: []
  key_links: []
---

<objective>
Validate the full build and test suite after test migration.

Purpose: Plans 01 and 02 rewrote/updated all three test files. This plan runs the full build and test suite to confirm everything compiles and passes. If there are compilation errors or test failures, this plan fixes them -- it is the final validation gate for the entire Phase 3.

Output: A passing `pixi run build` (all 61 targets) and `pixi run test` (all tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-test-migration-and-build-validation/03-01-SUMMARY.md
@.planning/phases/03-test-migration-and-build-validation/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no synchronized_data_batch references remain in tests</name>
  <files></files>
  <read_first>
    - test/data/test_data_batch.cpp (verify Plan 01 output -- should have zero synchronized_data_batch)
    - test/data/test_data_repository.cpp (verify Plan 02 output -- should have zero synchronized_data_batch)
    - test/data/test_data_repository_manager.cpp (verify Plan 02 output -- should have zero synchronized_data_batch)
  </read_first>
  <action>
Run a grep across the entire test directory to confirm zero references to the old type:

```bash
grep -r "synchronized_data_batch" test/
```

This must return no matches. If any references remain, fix them by replacing `synchronized_data_batch` with `data_batch`.

Also verify that the test files have the expected new-API patterns:
```bash
grep -c "data_batch::to_read_only" test/data/test_data_batch.cpp
grep -c "make_shared<data_batch>" test/data/test_data_repository.cpp
grep -c "make_shared<data_batch>" test/data/test_data_repository_manager.cpp
```
  </action>
  <verify>
    <automated>cd /home/bwyogatama/cuCascade/.worktrees/ws-c30649ca-49d8-4c87-89e2-90c8df69b38c && grep -rc "synchronized_data_batch" test/ | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "synchronized_data_batch" test/ | grep -v ":0$" | wc -l` returns 0 (no files with old references)
    - `grep -c "data_batch::to_read_only" test/data/test_data_batch.cpp` returns a positive number
  </acceptance_criteria>
  <done>
    Zero synchronized_data_batch references remain in the test directory. All three test files use the new data_batch type exclusively.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full build and test validation</name>
  <files></files>
  <read_first>
    - test/data/test_data_batch.cpp (quick scan for obvious syntax issues before building)
  </read_first>
  <action>
Run the full build and test suite:

1. **Build all 61 targets:**
```bash
pixi run build
```
This must complete with zero errors. If compilation fails:
- Read the error output carefully
- Fix the specific file causing the error (most likely test_data_batch.cpp since it was a full rewrite)
- Common fixes: missing `std::move`, wrong accessor syntax (`.` vs `->`), wrong template argument, missing include
- Re-run `pixi run build` until clean

2. **Run all tests:**
```bash
pixi run test
```
This must pass with zero failures. If tests fail:
- Read the CTest output to identify which test case failed
- Fix the assertion or test logic
- Re-run `pixi run test` until clean

3. **If build errors reference `synchronized_data_batch` in any file outside test/:**
- This means there is a reference elsewhere that Phase 1 or Phase 2 missed
- Search for it: `grep -rn "synchronized_data_batch" include/ src/`
- Fix by replacing with `data_batch`
- Re-build

**Expected outcome:** Zero compilation errors, zero test failures.
  </action>
  <verify>
    <automated>cd /home/bwyogatama/cuCascade/.worktrees/ws-c30649ca-49d8-4c87-89e2-90c8df69b38c && pixi run build 2>&1 | tail -5 && pixi run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `pixi run build` exits with code 0 (all targets compile)
    - `pixi run test` exits with code 0 (all tests pass)
    - Build output contains "Build finished" or equivalent success indicator
    - Test output shows "All tests passed" or "0 failures" or equivalent
    - No warnings about deprecated `synchronized_data_batch` usage
  </acceptance_criteria>
  <done>
    Full build passes (all 61 targets compile cleanly) and full test suite passes (all tests execute with zero failures). The data_batch refactor is validated end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Build/test validation only -- no code changes unless fixing compilation errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | N/A | Build system | accept | Running existing build/test commands -- no new attack surface. Build errors fixed in test code only. |
</threat_model>

<verification>
- `pixi run build` exits 0
- `pixi run test` exits 0
- `grep -rc "synchronized_data_batch" test/ | grep -v ":0$" | wc -l` returns 0
</verification>

<success_criteria>
The full cuCascade build compiles all 61 targets with zero errors and the complete test suite passes with zero failures. This proves the Phase 1 (core type system), Phase 2 (repository integration), and Phase 3 (test migration) refactoring is correct end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/03-test-migration-and-build-validation/03-03-SUMMARY.md`
</output>
